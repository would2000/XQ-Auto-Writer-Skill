# 更新紀錄

本文件記錄 XQ Auto Writer Skill 的重要變更。版本號遵循 [Semantic Versioning](https://semver.org/)。

## [Unreleased]

## [1.1.0] - 2026-08-02

- 修正乾淨 Windows CI 的依賴與隔離：正式宣告 Pillow 截圖／畫面辨識依賴，並讓 runtime evidence 單元測試使用獨立系統暫存根目錄，不再假設本機 `.xq-auto-writer/` 已存在或寫入真實私有狀態。
- 修正乾淨 clone／GitHub CI 無法載入第十一階段案例來源：將八份不含私人資料且已由公開 SHA-256 識別的最小 XScript 移入追蹤中的 `runtime-evidence-sources/`，案例不再依賴被忽略的本機 `generated/`；同步更新 suite digest，並新增公開證據與目前案例、來源雜湊一致性測試。來源內容與既有 XQ 編譯證據的 SHA-256 完全不變，未重新解讀或推測執行結果。
- 完成第十一階段五類代表執行證據與 1.1.0 發布準備：新增可續跑的四組函數／caller runner、JSON／JUnit／Markdown 私有彙總及拒絕覆寫的公開蒸餾器；2026-08-02 在 XQ 3.19.03 重新取得八份文件當次編譯成功，指標匯出 99 列、選股成功 50／失敗 0／交易 32、警示成功 1／失敗 0／交易 1、自動交易成功 1／失敗 0／交易 8，三份回測報告均以唯一 marker 證明後清理且最終無 checkpoint。修正 Windows 子程序 UTF-8 JSON、指標 `自訂/CODEX/` 選擇、自動交易顯式腳本身分驗證，以及 PowerShell 5 UTF-8 BOM 發布證據讀取；新建 `rc-interface-v3.json` 與不含私人資料的 `xq-runtime-evidence-v1.json`，保留已發布 v1／v2 契約。以上只證明代表流程可執行，不代表策略績效。

- 完成第十階段既有 CODEX 腳本端到端閘門：新增 `xq_existing_script_pipeline.py`，以精確名稱、類型、`自訂/CODEX/` 與來源 SHA-256 綁定開啟及當次編譯；支援函數先於 caller 的依賴順序，任何編譯失敗都在下游前停止，成功後重新確認主文件再交接既有指標、選股、警示或自動交易工具。2026-08-02 已在 XQ 3.19.03 驗證四組函數搭配四類 caller 共八份文件皆為 0 錯誤、0 警告，並以 `CodexV1FlowAutotrade` 完成公開商品 2330 的回測設定乾跑與取消；未按開始、未產生報告或 checkpoint，不宣稱執行結果或績效。既有 1.0.0 凍結 CLI 契約未被改寫。

- 完成「安全切換五大頁籤並開啟既有腳本」：`xq_category_selector.py` 每次以公式 host 記憶體截圖偵測五個穩定頁籤、以原生 pane 交叉驗證作用中分類，單次節流點擊後要求目標分類與唯一 `自訂/CODEX/`；新 `xq_open_existing_script.py` 只在 CODEX 直接子文件完整名稱唯一時，以 TreeView 局部截圖單次雙擊並讀回名稱、類型與位置。兩者不保存固定座標、比例、成功截圖或私人清單。2026-08-02 已在 XQ 3.19.03 真實驗證指標、選股、警示、交易、函數五類與重複執行零輸入；初次 Enter 開啟 timeout 已留存 incident、停止輸入並改以受控雙擊解決，未推測錯誤碼。

- 2026-08-02 真實自動交易 smoke 已在函數與交易腳本各自當次編譯成功後，以公開商品 2330 完成：成功商品 1、失敗 0、交易 8，唯一 marker 報告與清理證據完整。`SetTotalBar(20)` 停用預載欄位時，runner 明確回報要求值但未套用；報告關閉確認晚到但已證明內容消失時保留 `late_wait_observed`。

- 抽出三類回測共用的 `xq_backtest_monitor.py`：警示、選股與正常自動交易回測共用單次 Start、`starting → running → late_report → completed`、隱藏進度基準、唯一新增報告、腳本名稱 marker 及精確報告清理。新增選股白名單校正 probe 與完整 runner；2026-08-02 已從真實 XQ 3.19.03 唯一讀回選股方向、日頻、日期、進出場價、停利／停損、最大持有、成本及 Print 控制項，並以系統範圍 smoke 取得成功 1,920、失敗 21、交易 494 的報告證據。XQ 未提供的 21 筆錯誤碼保留為 `null`；報告關閉確認晚到但已證明內容消失時會記錄 `late_wait_observed`，證據不足仍停止。自動交易在開啟設定前新增已編譯交易類型與 `自訂/CODEX/` 讀回；明確取消測試仍保留獨立 Stop／復原契約。

- 警示真實回測新增慢速桌面韌性狀態機：Start 改為只送出一次非阻塞正式按鈕命令，設定視窗延遲關閉時只做唯讀監控，並追蹤 `starting → running → late_report → completed`、隱藏進度與有界晚到報告。報告查找先用快速 Win32 對話框列舉再做局部 UIA 解析；只有唯一新增 handle、指定腳本名稱 marker 與完整摘要同時吻合才清除 checkpoint，報告不唯一、marker 不符或證據不足仍維持人工覆核。2026-08-02 的 2330 警示 smoke 已由單次 Start 完成，唯一報告回報成功商品 1、失敗 0、交易 1，並自動完成報告與 checkpoint 清理；這只證明流程運作，不代表策略績效。

- XQ 本機狀態讀取改為優先使用小範圍原生控制樹與精確目標視窗截圖；非伺服器步驟不再因完整 UIA 視窗掃描而長時間盲等，且截圖不得取代編譯、報告或 recovery 證據。

- 新增操作憲法第八條：五大類型 XScript 必須先取得同一來源版本的當次真實 XQ 編譯成功證據，才能進入回測、圖表、選股、策略雷達、函數整合或自動交易等使用環境；標題含「未編譯」時一律 fail closed。

- 警示回測 timeout 現在會在 checkpoint 的精確進度窗仍可見且健康時，先保存局部私有截圖與已顯示的商品執行狀態，再執行較慢的 recovery 探測；不會因此關閉進度、清除 checkpoint 或自動重跑。重新蒸餾官方自動交易錯誤文章，並記錄 XQ 3.19.03 進度窗實測 `回測執行異常(1)` 為回測代碼 1、無報告時報告錯誤碼仍為空。

- 修正 XQ 重啟後的人工復原：當舊 PID 已確定結束、無進度且新 XQ 主視窗健康時，即使 XScript 尚未重開也可安全封存並清除精確 checkpoint；其他證據不足狀態仍拒絕。

- 新增警示最小真實回測 runner：所有重大設定必須明確傳入並讀回，實體輸入套用精確前景保護，以報告 handle 基準及 checkpoint 防止誤認舊報告，完成後只清理本次新增報告，timeout 或等待事件則保留復原證據。

- 修正選股回測範圍契約：系統預設範圍可由 CODEX 依完整名稱讀回驗證；自選股組合只能由使用者手動選取，CODEX 不讀取、保留或變更組合名稱與內容。
- 新增三類回測範圍的共用前景守門：每次實體輸入前驗證精確父視窗 handle 位於 Windows 前景，被其他應用程式遮住、切換遭拒、視窗 disabled／hung 或對話框 timeout 時停止且不補送清理；另新增不填寫其他回測欄位的自動交易商品 scope-only dry-run runner。
- 新增需精確 run ID 與明確人工確認的 checkpoint 封存／解除工具；既有自動交易 stale-checkpoint 旗標改為安全拒絕，避免未經可稽核確認就清除復原狀態。
- 統一選股、警示與自動交易回測的商品範圍讀回契約；新增警示逐檔與選股市場／系統範圍的安全 dry-run runner，並保留既有 checkpoint 的人工覆核保護。

- 歷史回測商品範圍改為由本次指定集合完整覆寫：只清空回測商品選擇器的暫時選取，讀回為空後再驗證最終集合完全相符；不修改自選股、群組、帳號、策略或 XScript 文件，且未明示的預設範圍仍不會自行推測。

- 新增 XQ 1--10 級操作節奏；預設第 5 級，使用第 7 級時在未觸及工作流安全下限的動作間隔約快 50%。節奏只影響輸入間隔與鍵盤送字速度，不縮短 timeout、晚到對話框判定、唯讀輪詢或函數回測的 2 秒安全下限。

- 修正 XQ 收尾視窗規則：只要求 XQ 主程式視窗最大化（非全螢幕）；保留的 XScript 編輯器與其他子視窗維持原本大小即可。

- 修訂 XQ 操作憲法：操作與學習可按需截圖，但知識完成蒸餾並納入知識庫後，必須精確刪除本次建立且已非必要的暫存截圖與視覺探測檔；只允許為未解 incident 或缺少替代證據保留最少量私有影像。另要求 AI 僅回報軟體設定、執行流程、實際結果與證據限制，不預設策略／腳本立場，也不從編譯、回測或訊號推論策略可行性或投資價值。

- 已在 XQ 3.19.03 將主圖疊圖 `TwentyDayNewHighBuyPoint` 的 `20日新高買點`正式設為小型實心紅色向上箭頭；提交後重新進入設定，讀回`點`型式、向上箭頭圖示及紅色色票，並確認技術線圖圖例同步變紅。

- 修訂 XQ 操作憲法的任務收尾畫面：切回 ChatGPT／Codex 前，必須以 Windows 正式命令將應保留的 XQ 主視窗最大化但不得進入全螢幕，並以 `IsZoomed` 或等價狀態讀回；切回 Codex 後仍須確認 XQ 視窗存在且保持最大化。

- 實測 XQ 3.19.03 指定技術線圖的主圖疊圖繪圖樣式：確認型式為線條、線段、柱圖、點；記錄線型／粗細、柱圖五種樣式，以及點圖示的最近／系統／自訂來源、匯入、自訂大小與內建向上箭頭。`TwentyDayNewHighBuyPoint` 的 `Low` Plot 可配合點型式的向上箭頭呈現離散買點；本次探索以取消離開，未套用設定。另記錄遞迴列舉該設定視窗可能造成 XQ 3.19.03 前端於 `COMCTL32.dll` 異常，後續應採語意探測後的單步畫面驗證。

- 已實測並記錄指標主圖疊圖的第一條入口：在已編譯的 XScript 指標文件頁籤右鍵選取「新增主圖疊圖」。文件說明自繪 MDI 頁籤與選單的受控定位邊界、主圖／副圖選項不可混用，以及加入後以主圖圖例讀回指標與 Plot 名稱的驗證方式。

- 已將 XQ 3.19.03 自動交易回測的執行商品流程納入交易工作流：讀回商品／組合／選股等來源與商品設定器的市場、類別、產業／組合連動；驗證全部加入、代碼查詢單一加入及完成回到回測設定，並要求開始前先檢查腳本具有進場與出場（或部位加減碼）動作。

- 已將 XQ 3.19.03 警示回測的商品來源與選擇流程納入警示工作流：可讀回商品／組合／選股等來源、公開市場分類與產業組合的相依關係、全部加入及個別查詢加入的驗證方式；回測前必須詢問停利與停損。

- 已將 XQ 3.19.03 選股回測的商品範圍實測流程納入選股工作流：讀回市場別預設「台股」、範圍預設「普通股全部(系統)」，其他範圍交由使用者自行選取；並將停利、停損與最大持有期（期＝日頻交易日）列為回測前必須明確定義的設定。

- 新增選股、警示與自動交易的回測設定契約，明確區分可告知後採用的預設值與必須由使用者確認的商品、盤別、頻率、停利停損、日期及分鐘 K 收盤處理；回報一律維持客觀，不推論策略可行性。
- 修正 XQ 新增腳本視窗的函數類別選取：改用可讀回狀態的原生按鈕點選，使函數、指標、選股、警示與交易共用選取流程一致。
- 建檔前置流程現會先以無副作用 dry-run 驗證各類腳本的 `自訂/CODEX/` 範圍；驗證成功後才正式建立文件，避免未驗證的範圍造成重複或錯誤的資料夾建立。

- 修正自動交易回測的商品選取：若 XQ 回測商品組合已含使用者指定的精確商品，直接讀回採用而不重複加入；若載入未指定的既有商品，立即停止而不清空、替換或修改該選取內容，避免 XQ 的重複商品提示或私人內容異動。

- 修正 `xq_prepare_script.py --dry-run`：缺少類型專屬 `自訂/CODEX` 時僅回報 `would_create_folder: true` 並取消，不再由 dry-run 建立資料夾；只有正式建檔流程可以補建並讀回該資料夾。

- 實測選股中心的相鄰歷史日期回溯：正式「上一日」控制 `20755` 每次切換會重新執行已驗證的 CODEX 策略，必須等待啟動／停止狀態完全復原並交叉檢查兩類結果。並記錄原生日曆程式化日期設定不會觸發 XQ 結果日期事件，日期格在當前版本存在邏輯／像素座標差異，流程改為 fail-closed，不將未讀回的日期誤作執行結果。
- 新增選股中心指定歷史日期的實測控制契約：結果區日曆按鈕 `20757` 開啟「選擇指定日期」視窗，原生日曆為 `SysMonthCal32`／`20714`，並以同日期設定、讀回及取消驗證。文件明定未取得使用者明確日期不得自行執行；日後執行時必須記錄篩選日期與兩類結果，且只允許已驗證的 `自訂/CODEX` 策略。
- 實測並記錄選股腳本從 XScript 編輯器「加入選股」建立基本策略，再移至選股中心 `自訂/CODEX` 的安全流程：新增視窗的腳本與公開範圍均先讀回；策略清單唯一篩選後以語意 `移動...` 命令選取 `CODEX`，最後從目錄、策略列與右側腳本三處交叉驗證。未執行選股、未接觸私人策略或帳號設定。
- 依使用者明確授權修訂 XQ 操作憲法：語意控制仍為第一優先；只有自繪控制項經 UIA、MSAA、Win32 與正式命令探測後仍不可操作時，才允許目前視窗內單步、節流、局部相對且前後可驗證的座標點擊。座標例外永遠排除登入／登出、真實帳號、券商串接、帳號設定、實單、刪改單、實際部位與庫存同步，且不放寬私人內容、CODEX 專區、manifest 或刪除證據。
- 實際依技術線圖右鍵設定路徑，將 `MyTrendScoreIndicator` 的 `強勢趨勢分數`由紅色線條改為紅色點；自繪型式彈窗只在當次矩形內使用單步局部相對定位，並由儲存格與技術線圖交叉驗證。結果保留完整橘色趨勢線、以離散紅點標記強勢日期，且不再跨非強勢區間連成紅線。
- 實際加入並觀察 `MyTrendScoreSegments` 真正換色候選版；確認兩個 `PlotLine(..., add := 1)` 序列會產生跨區間斜線、近垂直跳回與明顯失真的負值尺度，沒有沿同一趨勢分數逐段橘紅接替，因此將該版本標記為呈現失敗，不再把它當成真正換色模板。
- 修復 `MyTrendScoreSegments`：在重新計算首 Bar 以 `add := 0` 清除兩組保留線，再將一般與強勢 PlotLine 的繪圖型式都設為`線段`。此版已在 XQ 3.19.03 實際編譯成功並加入副圖驗證，尺度恢復正常，橘紅線段沿同一分數軌跡交替，且不再跨缺口延伸或產生垂直跳回。
- 將 `MyTrendScoreSegments` 改為台股紅漲綠跌的高對比配色：一般趨勢線使用純綠 `RGB(0,255,0)`，強勢趨勢線使用純紅 `RGB(255,0,0)`；已在黑色副圖背景實際驗證兩色沿同一分數軌跡清楚交替，且不影響既有線段中斷與正常尺度。
- 新增 `plot-style-learning-guide.md`，記錄 XQ 3.19.03 左下角 `設定` 按鈕（Automation ID `45017`）及「繪圖設定」視窗的控制契約、三個分頁、Plot 清單欄位與自繪儲存格的安全邊界；補入技術圖 Context Menu 鍵、具名 `設定...`、`技術分析設定` 的副圖與四個明細分頁，以及中央 `MFCGridCtrl` 的讀回限制。重新觀察 `MyTrendScoreIndicator` 後撤回互斥 Plot 可形成分段換色的錯誤結論，並確認兩條路徑最後都進入同一個無可及性選項的自繪型式彈窗。

## [1.0.0] - 2026-07-25

### 新增

- 新增 `xq_generate_basic_script.py`，可逐次產生指標、選股、警示、函數或自動交易的最小 UTF-8 `.xs` 來源，拒絕覆寫既有檔案，並以單一 JSON 說明尚未執行 XQ 編譯。數值函數範本可讓其他四類各自用 `--with-function --function-name` 產生最小 caller；自動交易 caller 不含下單敘述。
- 新增唯讀 `xq_category_selector.py` 與五類自繪公式頁籤的正式命令校正。XQ 3.19.03 的 Win32、Raw UIA、MSAA 與「檢視(V)」選單都未暴露可唯一操作的分類命令，官方文件也只說明滑鼠左鍵而未列分類快捷鍵；因此選擇器只在使用者已切到目標分類且唯一讀回 `自訂/CODEX` 時成功，其他情況回傳 `manual_switch_required`、`input_sent: false`，不把 pane ID 猜成 `WM_COMMAND`、不顯示／隱藏子窗格也不使用座標。
- 新增 `advanced-learning-guide.md`，依使用者明確要求逐頁蒸餾 XQ 學習地圖「XQ進階應用」側欄全部 17 篇內容頁。知識涵蓋經濟指標行事曆、自設畫面、美股全部時段、交易訊號標記、籌碼／市場籌碼／券商／股權分析、籌碼選股、族群透視及分點指標；全部標示為文件蒸餾，`body_text_stored` 維持 `false`，不保存官方正文、HTML、圖片、完整表格、完整範例或歷史個股／券商名單。Skill、官方來源、相關操作指南、AGENTS 與 README 已加入路由，且帳號、刪單與下單說明不放寬憲法限制。
- 新增 `xspractice-learning-guide.md`，依使用者明確要求逐頁蒸餾 XQ 學習地圖「XS語法應用」從「XQ 18.01 版本 XS 重點功能」到「計算區間漲跌幅的自訂函數」共 7 頁。知識涵蓋美股延長時段的版本限制、商品代碼／群組參數、編譯警告、`SetTotalBar`、指定頻率 `SetBarBack`、變數序列、`Print`、`OutputField`／`GetFieldDate` 及日期區間函數邊界；全部標示為文件蒸餾，`body_text_stored` 維持 `false`，不保存官方正文、HTML、圖片或完整範例。Skill、官方來源、函數／除錯／選股指南、AGENTS 與 README 已加入路由。
- 新增 `screener-learning-guide.md`，依使用者明確要求逐頁蒸餾 XQ 學習地圖「XS選股」從「選股因子分析：用數據說話，打造高勝率選股策略」到「用XS寫籌碼集中度的選股策略」共 13 頁。知識涵蓋選股中心、每日清單、欄位／腳本條件、自訂排行、`OutputField` 排序、因子分析、回溯、回測、題材型設計模式及完整選股錯誤碼；全部標示為文件蒸餾與文章版本，`body_text_stored` 維持 `false`，不保存官方正文、HTML、圖片、完整範例或文章股票名單。Skill、官方來源、選股實測指南、AGENTS 與 README 已加入路由，並明定只能記錄 XQ 實際回報的錯誤碼。
- 新增 `sensor-learning-guide.md`，依使用者明確要求逐頁蒸餾 XQ 學習地圖「XS策略雷達」從「策略雷達基本操作」到「策略雷達與雷達回測的錯誤代碼說明」共 15 頁。知識涵蓋警示生命週期、四種觸發節流、逐筆／自動洗價、基準商品與跨商品／跨頻率、雷達回測設定與報告、手機傳送、下單／平倉安全邊界，以及即時雷達與回測兩套錯誤碼；全部標示為文件蒸餾與文章版本，`body_text_stored` 維持 `false`，不保存官方正文、HTML、圖片、完整範例或 `xstrader` 外站內容。Skill、官方來源、警示實測指南與 README 已加入路由，且文件中的帳號／下單說明不放寬憲法的登入與實單禁令。

### 變更

- 依使用者決定移除臨時空白腳本輔助分類切換流程及其 CLI；處理特定分類前改由使用者先手動切換，CODEX 僅以唯讀 selector 驗證分類與唯一 `自訂/CODEX` 範圍。這避免依賴未暴露語意操作的文件頁籤。

- 修改 XQ 操作憲法的任務結束畫面規則：撰寫／編譯腳本保留成果腳本及 XScript，資料夾或腳本整理保留編輯器相關位置，指標展示保留技術線圖；這些相關 XQ 視窗不因收尾而關閉，但任務完成的最後前景一律切回 ChatGPT／Codex。`AGENTS.md` 的桌面收尾與完成定義已同步，並要求驗證 ChatGPT／Codex 前景及應保留的 XQ 視窗仍存在。
- `xq_prepare_script.py` 改用新增腳本的類型限定「儲存位置」路徑：先選類型，再於唯一 `自訂` 根節點檢查或補建直接子節點 CODEX，選取後要求 Edit `30023` 精確讀回 `自訂/CODEX/` 才能繼續。新增重名、錯誤位置、缺校正與 dialog timeout 的 fail-closed 契約及單元測試；真實 XQ 3.19.03 已驗證交易缺少 CODEX 的建立分支與函數既有 CODEX 分支，兩類 dry-run 都成功且沒有測試文件。

## [0.3.0] - 2026-07-24

### Changed

- 新增第九階段「發布候選驗證與維護模式」：以不含私人資料的 `rc-interface-v1.json` 凍結 `0.3.0` 候選的公開 CLI、schema／runner contract 與必要入口；候選準備期間正式 `VERSION` 維持 `0.2.0`，發布 PR 才升為 `0.3.0`。加入唯讀 RC 檢查器、私有原子維護狀態，以及從不可變 `v0.2.0` 在臨時目錄執行的升級／byte-level 復原演練；CI 取得完整 tag 歷史並新增兩項 RC 閘門。操作手冊明列完整回歸、故障復原、發布 PR／Draft Release 邊界與真實 XQ 前置條件；五類 `自訂/CODEX/` 選擇器未完成前，真實完整 RC 回歸仍保持 blocked，不改用私人內容、不推測錯誤碼。
- 新增唯讀 `xq_category_observer.py` 與五類自繪公式頁籤的人工切換校正：使用者依序停留指標、選股、警示、交易、函數後，XQ 3.19.03 穩定讀回公式窗格 control ID `1、4、2、7、3` 及各自唯一 TreeView `45242`；觀察器不送輸入、不用座標且不保存私人節點名稱。此證據只建立分類對照，尚未證明正式切換命令；當次只有指標已有唯一 `CODEX (0)`，其他四類仍須安全建立。
- 新增唯讀 `xq_folder_observer.py` 與 XScript 自訂資料夾校正紀錄：不送輸入、不用座標、不保存非白名單文字；真實 XQ 3.19.03 觀察證明公式 TreeView `45242`、標準 `#32768` 右鍵選單與「新增資料夾」，並在使用者手動重新命名後精確讀回目前分類的 `自訂/CODEX/`。建立對話框與其他四類分類選擇器尚未完整觀察，仍維持 fail-closed。
- XQ 操作憲法新增全視窗零座標規則：禁止固定、相對、矩形計算及幾何猜測座標，也禁止傳入 `coords`；只能以讀回的 Win32／UIA 控制項識別、階層、狀態或正式命令操作，缺少穩定選擇器時必須停止校正。
- 新增最高優先的 `docs/XQ-OPERATION-CONSTITUTION.md`：原生 XQ 私人內容只允許複製與唯讀讀取；五類 XScript、選股中心、策略雷達及自動交易項目必須隔離在各功能的 `CODEX` 專用資料夾；全面禁止 XQ 登入／登出、實際證券帳號串接與實單，只允許可明確識別的內建模擬帳號。憲法另固定慢速操作與 incident 證據、manifest 範圍清理、任務後視窗復原及 Print 新資料夾輸出規則，並由 `AGENTS.md`、Skill 與 README 強制引用。
- 新增函數整合測試自動化第八階段的 fail-closed 基礎：`xq_function_batch_runner.py` 將完整矩陣拆成逐 pair 子執行，前後唯讀 recovery 健康閘門、持久 cooldown、caller-stable child run ID、結果 digest 與安全續跑，並拒絕混合版本／digest、缺 pair、重複案例、清理不完整及等待事件。XScript 建檔與 manifest 清理現在必須精確讀回 `自訂/CODEX/`；缺少唯一校正選擇器時在接觸 XQ 前停止，不再接受 `自訂/` 根目錄。2026-07-24 乾跑已驗證兩組 pair 計畫；目前可見分類後續已建立並讀回 CODEX，但五類選擇器仍未完整校正，所以真實完整矩陣與不可變 baseline-v2 保持未執行，未繞過憲法、未推測錯誤碼。
- 完成函數整合測試自動化第五階段：案例 schema v2 分離來源索引與 caller `[n]`，加入分鐘／日頻、D／W／M、固定／動態、Default 分支預期、SetTotalBar、SetBarBack 及預載啟停代表矩陣；manifest 保存逐案狀態與 suite digest，明確續跑時不重跑完成案例。新增原子 JSON／JUnit 彙總與必要晚到報告演練，真實 XQ 3.19.03 已完成 8／8 案與 0.05 秒晚到報告復原，仍只有唯一新增 handle 且實際 marker 完全吻合才清除 checkpoint；XQ 文件、報告、暫存及 checkpoint 已完成 manifest 範圍清理。
- 新增函數整合測試自動化第六階段：正規化且不含私人執行資料的不可變 baseline、XQ／案例 schema／runner contract 版本鎖定、JSON／JUnit／Markdown 差異摘要及受影響 pair 重跑參數。boundary runner manifest schema v3 保存選取 pair，控制與不足案不可拆分；baseline 更新須明確確認、遞增版本並保留舊檔。加入 7818／2330 有限 smoke 矩陣，以及行程中斷、未證實斷網、報告不唯一與證據不足的非破壞性單元測試。XQ UI 操作新增至少 2 秒步驟等待、可調案例間隔及 `windows_wait_incidents` 快照；真實 XQ 3.19.03 run `6c85d6c1-c8b2-4ad8-9986-feacd82d4cb5` 完成 4／4 案，清理 timeout 後以同一 manifest 安全續跑且未重跑完成案，最終完整清理並建立 `baseline-v1.json`。
- 新增函數整合測試自動化第七階段：runner contract 7／manifest schema v4 將 XQ action settle、輪詢退避、對話框晚到／timeout、視窗狀態 timeout 與案例間隔全面參數化，續跑不得加速；Ctrl+O 晚到不再補送輸入。逐文件清理狀態機原子保存開啟、名稱／類型／自訂位置讀回、刪除確認、刪除後不存在與完成，重複清理、已不存在及 incident resume 都不重跑已完成案例或文件。等待事件保存 UTC、案例、文件、階段、PID、視窗健康、checkpoint、可見報告及唯讀 recovery-status。回歸工具新增 `*-plan.json`，只對同版本差異產生安全的成對 `--only-pair` 計畫；版本不一致要求完整矩陣。真實 XQ 3.19.03 run `3449ecb6-8822-4ae1-b91e-c6bfa6b73f16` 已完成一組慢速代表 pair／2 案、四份文件逐件清理與零等待事件，不做當機或斷網注入。
- 完成函數整合測試自動化第四階段：新增 JSON 資料邊界案例 runner，自動配對較短足量控制組與唯一 marker；回測 checkpoint schema v2 保存開始前報告 handle 基準，逾時後僅允許唯一新增報告且指定 marker 實際吻合時復原，否則維持人工檢視。結果保留編譯、商品、交易、實際錯誤碼、marker、執行與設定證據，並以 manifest 名稱／類型／自訂位置讀回限制 XQ 文件、報告與暫存清理；報告清理另處理未儲存提示並驗證報告內容消失，不把單純 close 請求當成完成。
- 完成函數資料不足第三階段：以 2330 與 2026-05-18 掛牌的 7818 驗證日／週／月來源頻率、足量／差一根／明顯不足、固定／動態索引、Default 對照，以及 `SetTotalBar`／`SetBarBack` 交互作用。`xq_backtest.py` 現在遇到 XQ 因 `SetTotalBar` 停用預先執行筆數欄位時不再強制寫入，並以 `settings_evidence` 明列是否套用。

### 新增

- 完成函數資料不足與初始化切片：預載 0／1／5 的明確中性哨兵與 `[1]` caller 三次皆成功；日頻 100 根足量控制組取得預期 `1301` 路徑標記，而固定／動態 10,000 根回看雖顯示商品成功、交易 0，無條件哨兵卻未執行。新增規則要求資料邊界測試必須有足量控制組與執行路徑證據，且不得在報告未提供時猜測 `1401`、超界值或 `Default` 行為。
- 加入桌面任務收尾規則：完成視窗學習、XQ 操作、腳本編譯、回測或其他桌面動作後，關閉不再使用的子視窗，最後將 ChatGPT 軟體切回前景並驗證可見。
- 完成 1 分鐘 caller → 日頻序列跨頻率切片：真實 XQ 證明當日日頻 Close 隨分鐘 Close 更新、日頻 `[1]` 同日維持前一交易日收盤且資料日期正確；三份 V2 文件皆為 0 錯誤、0 警告，紅燈取得指定 `1301`，綠燈成功 1／失敗 0／交易 2，並新增 `Daily` 保留字前綴的編譯器規則。
- 完成 `SetBarMode(2)` 條件式呼叫切片：真實 XQ 證明受測函數 series 在 false 分支的 bar 仍延續，而 caller 接收變數只在條件為 true 時更新；診斷第 4 根取得 121 而非依呼叫次數推算的 114，正式紅燈取得指定 `1301`，綠燈成功 1／失敗 0，並完成失效進度 handle 後的 checkpoint 復原與乾淨重跑。
- 完成函數呼叫函數的巢狀契約切片：依序編譯內外兩層數值函數，以正反非對稱參數和 `NumericSeries[1]` 驗證完整展開結果；紅燈證明內層 `1301 / CODEX_NESTED_INNER_GUARD` 能穿透外層，綠燈成功 1／失敗 0／交易 2。
- 完成多維陣列與長度／越界函數切片：真實 XQ 驗證固定 2×3 陣列六個元素、一維動態陣列長度 1／4、零長度 `1301` 防護及未防護越界原生 `1303`；並記錄 caller 常數運算超過 32 位元有號範圍會造成 expected 溢位的測試陷阱。
- 完成 `TrueFalseSeries` 歷史傳遞紅綠燈驗證：以固定 `TTFF` 布林序列及 1／10／100 三根非對稱編碼，真實 XQ 編譯三份文件皆為 0 錯誤、0 警告；紅燈取得指定 `1301`，綠燈成功 1／失敗 0／交易 2，並明確區分輸入序列歷史與 `SetBarMode(2)` 函數自身狀態。
- 完成邏輯值與字串函數在指標、選股、警示三種呼叫端的實際執行矩陣：8 份 XScript 均為 0 錯誤、0 警告；兩個指標各 208 列原生匯出全數吻合，兩個選股各命中台灣 50 的 50 檔且錯誤 0，兩個警示皆為 1 筆觸發／0 筆不觸發，並完成測試文件、選股策略、雷達策略與子視窗清理。
- 加入 `xq_alert.py` 警示實際觸發／不觸發紅綠燈驗證：以唯一策略名稱、公開商品及策略雷達「單次洗價模式」建立參數 1 控制組，解析 `HH:MM:SS(N)` 觸發筆數，再複製為參數 0 對照組，要求執行完成且無觸發節點；預設精確讀回腳本與商品後只刪除本次策略，清理失敗即降級為 `automation_error`。已以 XQ 3.19.03、台積電及同一警示腳本真實驗證紅燈 1 筆、綠燈 0 筆與兩個策略完整清理。
- 加入 `xq_indicator.py` 指標實際執行擷取與比對：在可復原的臨時圖表頁精確加入已編譯 XS 指標，等待 XQ 原生 Excel 匯出完成後擷取 Plot 欄位，支援完整逐列仿射公式驗算、`success`／`mismatch`／`automation_error` 三態、Excel 多執行個體辨識、活頁簿收尾與書籤復原。已以 XQ 3.19.03、台積電日線及 `Close * 2 + 7` 真實驗證 179 列全數相符、最大誤差 0；刻意將期望偏移改為 8 時，179 列均正確回報錯配並完成復原。
- 擴充選股執行進階能力：每次完成後同步擷取「執行錯誤的商品」原生 CSV，輸出完整錯誤數、可截斷明細及不推測的錯誤代碼，並區分 `success`、`failure`、`partial_failure`；逾時時只在停止鍵可用時中止，確認啟動／停止控制項復原後回傳 `cancelled` 並略過舊結果。已以 XQ 3.19.03 真實驗證正常 50／錯誤 0、錯誤 50／命中 0，以及普通股全部逾時停止及復原。
- 加入 `xq_screener_pipeline.py` 選股一鍵管線，串接新文件建立、當次真實編譯、公開系統策略建立、執行及原生 CSV 結果擷取；任一階段失敗即停止並保留證據，僅對已確認未建立策略的搜尋框初始化錯誤有限重試一次，且只收尾本次新開的成功編譯器視窗。已在 XQ 3.19.03 以全新腳本與策略驗證 0 錯誤、0 警告及台灣 50 命中 50 筆。
- 加入 `xq_screener.py` 選股策略自動建立、實際執行與結果擷取：只使用公開系統範圍、拒絕同名覆寫，以無結果哨兵確認搜尋刷新，並在缺少腳本或失敗時取消子視窗；以真實 XQ 3.19.03 驗證穩定 command/control ID、CP950 原生 CSV、`OutputField` 欄位、固定命中 50 筆、成功空結果及退出碼 2，再轉為可截斷但保留完整命中數的單一 JSON。
- 加入函數腳本專用指南與 `xq_function_preflight.py` 離線契約檢查，涵蓋三種回傳型別、canonical header、`retval`、參數／計算模式／邊界行為、呼叫端整合驗證，以及圖表、報表與交易副作用攔截。
- 加入 `xq_function_integration.py` 紅燈／綠燈歷史整合測試，強制回測授權、逐階段建檔與編譯、唯讀復原檢查、精確 `1301` 標記及純成功綠燈判定；並在真實 XQ 完成數值、邏輯值與字串三種回傳型別，以及 `NumericRef`、`NumericArray`／`NumericArrayRef`、`SetBarMode(2)` 狀態延續的參數順序、序列傳遞、副作用與呼叫結果最小垂直切片。
- 加入 `xq_function_caller_matrix.py` 跨腳本 compile-only 驗證工具，在真實 XQ 建立並編譯同一函數的指標、選股及警示呼叫端，並將編譯相容與自動交易紅／綠執行證據分開標示。
- 加入 `xq_backtest.py` 公開商品回測 CLI，支援最多 20 個商品的逐筆代碼完全比對、欄位填入與讀回、安全安控、dry-run、指定秒數／完成商品數／逾時中止，以及依報告商品數量輸出的五態終態判定；中止可選擇不保留或要求已完成結果，監控與復原期限彼此獨立，並分別驗證核取方塊、部分報告、進度關閉及 XScript 復原，失敗報告則可擷取逐商品狀態、錯誤代碼與 XQ 原始說明。
- 加入回測環境復原第一、二階段：以 XQ 行程／視窗心跳分類行程退出、無回應、視窗遺失與 XScript 關閉，並用不含策略或帳戶資料的本機原子 checkpoint 記錄安全階段、阻止 stale 狀態下重複啟動及提供明確確認後的解鎖流程。
- 加入回測環境復原第三階段：提供完全唯讀的 `--recovery-status`、五態安全決策、原因與稽核證據、既有進度防重複啟動及可見報告摘要；既有報告與 checkpoint 的關聯未獲證明時不推定歸屬，並永久禁止自動重跑。
- 加入回測報告結構化擷取與匯出：可唯讀列舉可見報告，依 handle 明確選取，以白名單 schema 原子寫入私有 JSON／CSV，拒絕覆寫、回傳 SHA-256、避免 CSV 公式注入，並將失敗商品明細設為明確選用功能。
- 加入 XQ 原生回測檔案輸出：支援「儲存」`.BTReport`、「完整匯出」`.xlsx` 與「僅匯出交易紀錄」CP950 `.csv`；預設提議 Windows 實際桌面但強制先詢問並確認目的地，使用唯一檔名、拒絕覆寫、驗證格式與 SHA-256，並確認匯出完成提示及報告 UI 復原。
- 加入 `lesson/xsat` 左側「XS 自動交易」17 頁的完整蒸餾指南，涵蓋策略生命週期、`Position`／`Filled` 狀態機、歷史部位計算、回測撮合、週期分析、帳號與庫存同步、監控排程及 `CancelAllOrders` 非同步取消契約。
- 加入四篇 XQ 官方教學的跨主題蒸餾知識，涵蓋每日資料更新時效、警示中心狀態模型、`IsFirstCall` 四種事件與 `Print` 路徑、替代標記、雲端回測及安全除錯契約。
- 加入 XQ 自動交易與回測錯誤代碼的逐碼蒸餾知識，區分兩種流程的代碼差異、第一輪排查、最小重現、Print 除錯與安全問題回報契約。
- 加入真實 XQ 3.19.03 視窗驗證的自動交易入口與回測生命週期知識，記錄 XScript「加入自動交易」、「自動交易中心」、完整回測欄位、執行進度、完成報告、重新回測與設定缺件流程的穩定選擇器及安全邊界。
- 補齊真實 XQ 3.19.03 回測設定自動填入驗證，涵蓋固定欄位的寫入／讀回／還原、價格模式相依欄位、五種商品來源模式視窗、數值腳本參數內嵌編輯及可復原取消流程。
- 加入回測自動啟動的五態判定契約，實測摘要 0% 與商品明細「執行中」的差異、動態列控制項、受限等待、中止確認及 hidden 進度容器；重啟 XQ 後再驗證成功與執行失敗報告，改以報告中的商品成功／失敗數量區分 `success`、`failure` 與 `partial_failure`，未得到終態時仍保守分類為 `indeterminate_timeout`。
- 加入 XQ 學習地圖 13 篇自動交易教學的蒸餾知識，涵蓋策略建立、洗價模式、回測成交假設、庫存同步、帳號環境、監控排程、錯誤分類、Print 除錯與第一次洗價事件。
- 加入 XQ 官方交易語法專章的自動交易蒸餾知識，涵蓋 `Position`／`Filled` 狀態模型、`SetPosition` 執行方式、未平倉成本、成交紀錄、演算法委託模式與舊文範例限制。
- 加入前七批共 132 個 XSHelp 報價欄位的結構化蒸餾資料、涵蓋率 checkpoint 與本地搜尋工具，完整涵蓋常用、價格、量能、財務、市場統計、期權與五檔分類，並納入財務／營收期間、跨市場週期、曆日／交易日回看、還原價格、Tick 位移、單位、格式及使用時機。

### 變更

- 強化指標匯出對無關重名欄位的處理：後續同名欄位以穩定序號保留，但受測 Plot 或 expected 欄位重名時仍拒絕判定；修正警示紅綠燈複製流程在名稱不存在探測後失去原策略選取的狀態缺口，並加入階段化錯誤訊息。
- 調整 XSHelp 來源政策：一般腳本仍最多按需讀取三頁；專用知識維護可依 metadata 索引執行受控、分批、節流且可恢復的全站蒸餾，同時維持 `body_text_stored: false` 並禁止保存官方正文或完整範例。

### 修正

- 修正乾淨 clone／GitHub CI 缺少本機 `generated/` 函數邊界案例與 XScript 產物而失敗；v4／v5 公開歷史案例移入追蹤中的參考目錄，來源契約測試改由正式 runner 即時產生，不公開或依賴使用者生成腳本。
- 修正 XScript 文件名稱包含「錯誤」時，編譯擷取器可能把「編譯開始」誤判成編譯失敗的問題。
- 相容 XSHelp 現行表格型頁面結構，使暫讀工具能擷取欄位屬性，同時維持正文不落地。

### 安全性

- 將未來 GitHub Release 改為 Draft-first 發布，並要求以 Release Immutable 狀態與 GitHub attestation 驗證已發布版本完整性。

## [0.2.0] - 2026-07-19

### 新增

- 加入唯讀的 Windows GitHub Actions CI，在 Pull Request、`main` push 與人工觸發時驗證版本 metadata、儲存庫衛生、單元測試、Python 語法及 submodule 狀態。
- 加入單人維護流程與可重現的公開儲存庫檢查器。

### 變更

- 將專案治理預設調整為單人維護：PR 不要求第二人核准，但仍須通過 CI 才能合併。

### 修正

- 修正全新 Windows Python 環境缺少 IANA 時區資料，導致 XSHelp 同步無法解析 `Asia/Taipei` 的問題。

### 安全性

- CI 使用唯讀 GitHub 權限、不保存 checkout 憑證、不使用 secrets，且官方 Actions 固定至完整 commit SHA。
- 啟用 `main` Ruleset，要求 Pull Request 與 `verify` check，並禁止刪除及 force push。
- 啟用 `v*` Tag Ruleset，允許建立新版本 tag，但禁止更新或刪除既有 tag。

## [0.1.0] - 2026-07-19

### 新增

- 支援指標、選股、警示、數值／邏輯值／字串函數及自動交易腳本。
- 加入 XQ XScript 建檔、編譯、錯誤擷取與最多十次的修正閉環。
- 加入 `XScript_Preset` 與 `XQStrategy` 上游 submodule。
- 加入 48 個分類、1,459 筆文件的 XSHelp metadata-only 索引及按需暫時讀取。
- 加入 Windows UI 控制項探測、校正範本與校正指南。
- 加入版本中繼資料驗證、人工發布程序及 GitHub 協作模板。

### 安全性

- 排除本機 UI 設定、使用者生成策略、快取、秘密與未授權網站正文。
- 明確區分本專案 MIT License 與第三方 submodule／官方資料的權利範圍。
